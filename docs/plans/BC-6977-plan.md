# BC-6977 — flow-architecture `/flow:ship` (cloned + FDA-swapped)

> Source-of-truth: BC-6977 Linear issue body (Q53 sub-decisions 1-7 + acceptance criteria). This plan operationalizes the spec into ordered tasks. **Do not re-derive Q53 content — crib from the issue body / `project_fda_plugin_interview.md:1621-1678`.**

## Scope

Clone `plugins/workflows/commands/ship.md` (locked to v3.29.4 per Q53 sub-decision 1) into `plugins/flow-architecture/commands/ship.md` with the Q53 sub-decision 3 per-step FDA-swap classification:

- Steps 0, 7 — preserved verbatim.
- Steps 4, 5, 6 — preserved verbatim (TRANSITIVE REUSE per Q50 amendment 2).
- Steps 1, 2, 3, 8 — preserved + FDA augments (active swaps).
- Telemetry blocks stripped per Q50 sub-decision 5 axis 7.

## Tasks

### T1 — Pre-flight (re-verify source + read sibling cousin)

- Read `plugins/workflows/commands/ship.md` end-to-end. Confirm 9-step structure (`grep -c "^## Step [0-9]"` returns 9). DONE.
- Read `plugins/flow-architecture/skills/_shared/linear-writeback-pattern.md` for Q46 contract (interface signature, marker convention, type registry). DONE.
- Read `plugins/flow-architecture/commands/retro.md` for the Q46-routing precedent (Step 3 swap pattern mirrors retro's Q46 call). DONE.
- Read `plugins/flow-architecture/CLAUDE.md` § Boundaries + § Q46 writeback layer for cross-cutting alignment. DONE.

### T2 — Author `plugins/flow-architecture/commands/ship.md`

File layout:

1. Frontmatter — `description` mirrors workflows ship.md.
2. HTML-comment header (verbatim per Q53 sub-decision 1):
   `<!-- Cloned from workflows v3.29.4 (commands/ship.md) on 2026-05-07. Drift-detection per parking lot #45. -->`
3. Title `# Ship & Compound` (verbatim).
4. NO Telemetry: Start block (stripped per axis 7).
5. Step 0 — verbatim.
6. Step 1 — preserved checks + `/flow:audit --domain=<DOMAIN>` pre-flight augment + plan-X verification augment. Include exit-code handling (`exit 1`, `exit 2`, soft-gate, halt) + `Plan not yet generated` regex substring + redirect to `/flow:plan-<discipline>`.
7. Step 2 — preserved `gh pr create` mechanics + FDA-shaped PR description (milestone / sub-flow parent / 5 discipline children / story doc / journey doc / intent.md). Include the phrase `FDA-shaped` in body text.
8. Step 3 — FDA-swapped Linear write via `linear_writeback({issue_id, type: 'ship-summary', surface: 'comment', content, signature: '_Generated by /flow:ship for <issue-id> on <ISO-8601>_', breadcrumb_path, warn_on_clobber: true})`. Reference the `<!-- FDA-WRITEBACK-ship-summary-START -->` marker convention (lowercase kebab; the Q46 v1 type registry enforces this literal value). Status move + PR attachment stay direct.
9. Step 4 — verbatim (TRANSITIVE REUSE: `compound-learnings`).
10. Step 5 — verbatim (TRANSITIVE REUSE: `best-practices-audit`).
11. Step 6 — verbatim (TRANSITIVE REUSE: `handbook-drift-check`).
12. Step 7 — verbatim.
13. Step 8 — preserved Session Close summary + retro soft-notification: detect "last sub-flow in domain?" via Linear query (all sibling sub-flows in milestone completed?); if yes append `Consider running /flow:retro <DOMAIN> when ready.` to the summary.
14. Rules section — preserved + 1 added rule on Q46 routing.
15. NO Telemetry: End block (stripped per axis 7).

### T3 — Verify acceptance criteria

Run all 16 checks from the issue body:

| # | Check | Status |
|---|---|---|
| 1 | `test -f plugins/flow-architecture/commands/ship.md` | pending |
| 2 | `grep -q "Cloned from workflows v3.29.4"` | pending |
| 3 | `grep -c "^## Step [0-9]"` returns `9` | pending |
| 4 | `grep -q "/flow:audit"` | pending |
| 5 | `grep -q "exit 1"` | pending |
| 6 | `grep -q "exit 2"` | pending |
| 7 | `grep -q "halt"` | pending |
| 8 | `grep -qE "soft.gate"` | pending |
| 9 | `grep -q "Plan not yet generated"` | pending |
| 10 | `grep -qE "FDA-shaped"` | pending |
| 11 | `grep -q "FDA-WRITEBACK-ship-summary-"` | pending |
| 12 | `grep -q "type: 'ship-summary'"` | pending |
| 13 | `grep -q "_Generated by /flow:ship for"` | pending |
| 14 | `grep -q "compound-learnings"` AND `best-practices-audit` AND `handbook-drift-check` | pending |
| 15 | `grep -qE "Consider running.*/flow:retro"` | pending |
| 16 | `grep -vq "telemetry-log.sh"` (telemetry stripped) | pending |

### T4 — Plugin-level validation

- `./scripts/validate.sh` exit 0.
- `./scripts/check-guardrails.sh --claude-md plugins/flow-architecture/CLAUDE.md` exit 0.
- Bump `plugins/flow-architecture/.claude-plugin/plugin.json` version (per BC-6000 cache-propagation rule).
- Bump matching `.claude-plugin/marketplace.json` entry to the same version.

### T5 — Linear update + commit

- Move BC-6977 to `In Progress` (was Backlog) at start; `In Review` after PR.
- Single commit referencing BC-6977 + Q53 lock.
- Open PR with FDA-shaped description.

## Out of scope

Per the issue body's "Out of scope" section: v1.1 handbook-drift-check FDA-customization, workflows skill reimplementation (TRANSITIVE REUSE), Q53-specific gate v1.1 promotion (parking-lot candidate).

## Dependencies

All Done:
- BC-6955 (`skills/_shared/linear-writeback-pattern.md`) — Done 2026-05-11.
- BC-6972 (`/flow:retro`) — Done 2026-05-11.
- BC-6969 (`/flow:audit`) — Done 2026-05-12.

## Re-address before drafting

- Re-read Q53 (project_fda_plugin_interview.md:1621) + Q50 amendment 2 (memory:1598) + Q53 refinement audit trail (memory:1664). DONE.
- The `<DOMAIN>` lookup at Step 1 audit invocation derives from the Q24 mod 3 label parse on the shipping discipline-child (`domain:<slug>` label). Step 1 augment cites this.
- The "last sub-flow in domain" check at Step 8 is a Linear query over sibling sub-flows in the same milestone — not a filesystem check.
