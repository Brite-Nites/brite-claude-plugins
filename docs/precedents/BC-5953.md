<a id="BC-5953-task-1"></a>
## BC-5953 — 2026-04-22 — Column-target verification is a distinct factual-anchor check (recipe #8): at plan-gate, verify the write-target column exists in the specific table the skill appends to, not just that the source field exists

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- BC-5953 issue body § "What to change": "Populates the corresponding matrix row's **Notes** column with the pulled text"
- MSPA SKILL.md:129–135 (my Step 3.5 original, which obeyed the issue body literally and targeted `Notes`)
- MSPA SKILL.md:167 (MSPA Matrix Format — body matrix schema: `Market / Segment / Persona / Angle / Batch / Verdict / Notes`)
- MSPA SKILL.md:141 (Results Log schema — per-ITERATE append table: `Batch / Experiment / Reply Rate / Interested Rate / Bounce Rate / Verdict / Transferable Insight` — **no Notes column**)
- MSPA SKILL.md:252 "Matrix is append-only in steady state" — body matrix is never mutated post-MAP
- MSPA SKILL.md evals §9 `matrix-append-not-replace` — asserts body-matrix rows are byte-for-byte unchanged post-ITERATE
- Review-agent P1 [9/10] from code-reviewer: "Step 3.5 and line 146 disagree on which column gets populated ... a subagent following Step 3.5 literally writes to `Notes` (which lives in the body matrix — a table that §3 Step 4 explicitly says 'do not rewrite the body')"
- BC-5828 check #6 (plan-gate live-read) + BC-5829 check #7 (cross-skill schema contract) — the existing factual-anchor checks that caught the `transferable_note` field-name match but did NOT catch the target-column name mismatch

**Alternatives Considered:**
1. **CHOSEN.** Add a new factual-anchor recipe check #8: "At plan-gate, verify the write-target column exists in the specific table the skill will append to, not just that the source-side field exists." Applied retroactively here: the P1 fix retargeted Step 3.5 from body-matrix `Notes` (invariant-violating) to Results Log `Transferable Insight` (invariant-preserving AND semantically correct — the column name literally matches `transferable_note`'s intent). Reason: BC-5829 check #7 catches field-name drift between skills (e.g., `transferable_note` consumer must match producer), but a field-name match does NOT guarantee column-name match on the write side — those are two different schema contracts. When a skill appends to an artifact with multiple tables (body + Results Log in this case, both in `mmf-matrix.md`), the issue body's column-name reference is ambiguous unless the plan explicitly picks one table and verifies the column against it.
2. Extend BC-5829 check #7 to cover write-side column names too. Rejected — check #7's scope is cross-skill field names (producer-to-consumer). Write-side column names are a within-artifact concern on the consuming skill side, not a cross-skill contract. Conflating them dilutes check #7's specificity.
3. Rely on review-agent catch (status quo before this session). Rejected — the P1 was only caught at review-time, after the first implementation. A plan-gate check catches it before code is written, saving a review-fix-re-review cycle.
4. Add a linter rule parsing SKILL.md tables and cross-checking. Rejected — too brittle for prose that describes tables in multiple styles (inline, reference, conceptual). Human/LLM plan-gate check is more robust for this class.

**Outcome:**
- Files changed: `plugins/marketing/skills/message-market-fit/SKILL.md` (Step 3.5 retargeted, §3 Step 4 dual-source note, §6 Flow 2 step 6, §4 Receives-from bullet, §9 scenario renamed and rewritten), `plugins/marketing/skills/message-market-fit/evals/evals.json` (happy-path scenario rewritten with append-only co-invariant assertion added)
- Tests: 0 added, 0 passed, 0 failed (docs-only; verification via `./scripts/validate.sh` + guardrails + re-review)
- Approved by: auto-verified (`/workflows:review` Step 7 fix-loop — both P1s CONFIRMED RESOLVED by targeted code-reviewer re-run at confidence 9/10)

**Precedent Referenced:** Extends BC-5828 (check #6 plan-gate live-read) and BC-5829 (check #7 cross-skill schema contracts). Adds check #8 to the factual-anchor recipe. Generalizable anywhere a skill appends to an artifact with multiple tables sharing overlapping column names — or where the issue body's column reference is ambiguous across tables. Applies especially to ITERATE-style skills that write Results Log / per-batch rows alongside a body schema. CDR promotion candidate: the 3-check recipe (6+7+8) now covers the triangle of (file-body drift, producer-consumer field drift, within-artifact table-target drift).

**Tags:** pattern-choice, factual-anchor-recipe, column-target-verification, write-side-schema-contract, append-only-invariant, marketing-skill

---

<a id="BC-5953-task-2"></a>
## BC-5953 — 2026-04-22 — Third application of BC-5830 task-2 (false-analogy precedent cite) confirms the class is recurring — promote to a standing check in the factual-anchor recipe

**Category:** pattern-application
**Confidence:** 7/10

**Inputs:**
- BC-5830 task-2 original precedent: "/workflows:review Step 6 validators must verify the cited precedent, not just the claim" (docs/precedents/BC-5830.md#BC-5830-task-2)
- My Step 3.5 original draft (SKILL.md:129): "skip this step silently ... (per account-research `sf-unavailable-graceful-degrade` degradation pattern)"
- account-research SKILL.md:346: the cited scenario — "emits a one-line warning to the operator ('Salesforce MCP unavailable, proceeding with public-source facts only'), and does NOT halt"
- account-research SKILL.md:126 + :293: architectural rule "mark `sf_enriched: false` and proceed with a public-only artifact"
- Code-reviewer P1 [8/10] at review-time: "the cited precedent supports a visible-but-non-halting degrade, not a silent degrade ... same failure mode flagged in BC-5830's review"
- Opus validator CONFIRMED the P1 with "visible operator warning + degraded artifact state + no halt — invoking it to justify 'skip silently' is a false analogy"

**Alternatives Considered:**
1. **CHOSEN.** Drop the false-analogy cite; state silent-skip as a deliberate local choice with explicit rationale (missing learnings.md is the normal first-ITERATE steady-state, unlike SF-unavailable which materially degrades the artifact — a warning here would be noise, not signal). Reason: the behavior I wanted (silent skip) was correct; the cite was wrong. Citing something that doesn't match what you actually do is the failure mode the precedent names — fixing either the behavior OR the cite is sufficient; I fixed the cite because silent-skip is the right behavior for this context.
2. Keep the cite, change the behavior to match (emit an operator-visible warning + `learnings_enriched: false` frontmatter). Rejected — a warning on every first-ever ITERATE is noise, not signal; an `enriched` flag on the matrix frontmatter creates a new schema contract for a nice-to-have that doesn't need one.
3. Drop the silent-skip behavior AND the cite; halt if learnings.md missing. Rejected — would block first-ever ITERATE for any new entity until a debrief is written, which inverts the normal dataflow (ITERATE produces the debrief input, not the reverse).
4. Cite a more accurate precedent for silent-skip. Rejected — no existing precedent for "silent skip because missing is the steady state"; the behavior is local enough to state inline.

**Outcome:**
- Files changed: `plugins/marketing/skills/message-market-fit/SKILL.md` (Step 3.5 degrade-path rewritten to drop the cite + add reasoning)
- Tests: 0 added, 0 passed, 0 failed (docs-only; verification via Opus re-review)
- Approved by: auto-verified (Opus validator CONFIRMED the cite was a false analogy; targeted re-review after fix CONFIRMED RESOLVED)

**Precedent Referenced:** [BC-5830-task-2](BC-5830.md#BC-5830-task-2) — 3rd observed instance of this class this week (BC-5830 task-2 original, implicit in BC-5828 check #6 plan-gate live-read, now BC-5953 task-2). The pattern is: author cites precedent X to justify behavior Y, but X actually prescribes Z ≠ Y — without a validator that reads X and checks Y against it, the skill ships with a misleading provenance trail that compounds at every re-read. The `/workflows:review` Opus-validator step (Step 6) is now load-bearing for catching this class — two-of-three instances were caught there, not by the review agents themselves. Takeaway: the validator substep is not optional scaffolding, it is the primary catch mechanism for false-analogy cites. Worth promoting to the factual-anchor recipe as an explicit standing check: **"Every precedent cited inline in a skill body must be live-verified by reading the cited section at review time."**

**Tags:** pattern-application, false-analogy-cite, precedent-verification, review-validator, factual-anchor-recipe, class-recurrence-3
