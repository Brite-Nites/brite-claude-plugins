<a id="BC-5827-task-1"></a>
## BC-5827 — 2026-04-21 — Mode-dispatch cardinality overflow: N > 4 modes require arg-first + optional 4-option fallback, not a larger AskUserQuestion

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- `docs/plans/BC-5827-plan.md` §Prerequisites + §Tasks (12-mode allowlist)
- `plugins/marketing/skills/creative-angles/SKILL.md` §2 Gate 2 (2-mode AskUserQuestion works cleanly at N=2)
- `plugins/marketing/skills/situation-mining/SKILL.md` §2 (0 modes — no question needed)
- AskUserQuestion tool schema (maxItems: 4 on the options array)
- Issue body mode enumeration (profiles, competitors, growth, hiring, reviews, news, negativity, founders, c-suite, full, deep, people — 12 total)

**Alternatives Considered:**
1. **CHOSEN.** Arg-first mode resolution with tiered fallback. (a) If operator supplied `mode`, validate against 12-item allowlist and proceed. (b) If unsupplied, default to `profiles`. (c) If operator explicitly asks "which mode?", surface a 4-option AskUserQuestion with the composite representatives (`profiles`, `full`, `deep`, `people`) and note the other 8 single-mode options are available by direct argument. This respects the tool schema cap without losing discoverability — the composites are the cheapest useful entry points and the allowlist lives in prose. Reason: aligns with how situation-mining and creative-angles surface optional behavior (argument-first, question-fallback only on ambiguity); avoids a 3-question AskUserQuestion cascade that would fragment intent.
2. Three-question cascade: tier select (single/composite/people) → then sub-option within tier. Rejected — three UI turns to pick one mode; user friction unacceptable for the default "profiles" path which should be zero-turn.
3. Rely on `AskUserQuestion` with 4 options plus "Other" free-text. Rejected — the 8 omitted single-process modes become invisible to operators who don't read the skill body; discoverability loss.
4. Split the skill into 3 skills (company-research, people-research, deep-research). Rejected — violates the thin-orchestrator charter (scope doc §2.1); upstream Revgrowth 01 is a single skill with the same mode fan-out; fragmenting would also duplicate the SF-enrichment conditional three times.

**Outcome:**
- Files changed: plugins/marketing/skills/account-research/SKILL.md (§2 Gate 2 in Batch A commit 86d62fb)
- Tests: 0 added, 0 passed, 0 failed (pure markdown skill; evals §9 exercises mode dispatch via `mode-dispatch-distinct-for-each-mode` scenario, IDs verified 1:1 against evals.json)
- Approved by: auto-verified (review loop passed; code-reviewer + cdr-compliance confirmed mode allowlist + dispatch logic)

**Precedent Referenced:** none (new pattern — the first skill in this plugin to exceed AskUserQuestion's 4-option cap on a single mode decision; sibling skills stay at N ≤ 2 modes).

**Tags:** pattern-choice, mode-dispatch, askuserquestion-cap, arg-first-resolution, marketing-skill

---

<a id="BC-5827-task-2"></a>
## BC-5827 — 2026-04-21 — BC-5828 factual-anchor check #6 (live-read at Plan gate) caught two process-file mapping ambiguities before the skill was written

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- `docs/precedents/BC-5828.md` §Decision — "live-read referenced file bodies at Plan gate" is check #6 of the factual-anchor recipe
- Issue body mode enumeration (9 single-process modes named; "deep" defined as "all 9 company processes")
- `ls plugins/marketing/references/research-processes/` — actual disk contents show 16 find-*.md files, not 9
- Live reads of `find-pr-releases.md`, `find-job-role-insights.md` during Plan gate

**Alternatives Considered:**
1. **CHOSEN.** Apply BC-5828 check #6 at the Plan gate: live-read all 16 process files before drafting §3, surface ambiguities in the plan doc (find-pr-releases and find-job-role-insights), decide each with a documented rationale, and only then write §3. Outcome: `find-pr-releases` folds into `deep` composite (company-level, no argument dependency); `find-job-role-insights` is excluded from all composites because it requires `role_title` which mode-dispatch can't supply — exposed only via direct invocation in §6 Flow 4 as a role-specific follow-up path. Both decisions were documented in the plan's Plan-gate live-read section AND in §3 Plan-gate scope note in the skill. Zero drift downstream: Batch B wrote §3 without inheriting the issue-body ambiguity.
2. Draft §3 using the issue-body mode list verbatim ("9 company processes"), interpret "9" as whichever 9 I noticed first, hope the operator doesn't notice. Rejected — this is exactly the BC-5828 failure class (plan-inherited factual-anchor drift). Not applying check #6 when the directory count (16) doesn't match the issue-body prose count (9) is negligent.
3. Defer the ambiguity to implementation, raise a question to the operator mid-Batch B. Rejected — mid-batch surface questions fragment the subagent dispatch; Plan gate is the cheapest place to resolve.

**Outcome:**
- Files changed: docs/plans/BC-5827-plan.md (§Plan-gate live-read section), plugins/marketing/skills/account-research/SKILL.md (§3 Plan-gate scope note, §6 Flow 4 role-specific follow-up path)
- Tests: 0 added, 0 passed, 0 failed (validation checks in Batch B subagent: find-pr-releases appears ≥ 2 times / find-job-role-insights appears exactly 1 time / Plan-gate scope note renders)
- Approved by: auto-verified (Batch B subagent's 7 verification checks all passed on first run; code-reviewer + cdr-compliance found no factual-anchor issues downstream)

**Precedent Referenced:** BC-5828 (factual-anchor recipe check #6 — live-read referenced file bodies at Plan gate). First successful application of check #6 to catch plan-inherited drift BEFORE the skill body was written. Extends BC-5797 factual-anchor recipe by confirming that the directory-count-vs-prose-count discrepancy is a reliable trigger signal for check #6 activation (when an issue body names N files but the referenced directory holds M > N, always live-read all M).

**Tags:** pattern-choice, factual-anchor-recipe, plan-gate, live-read, check-6
