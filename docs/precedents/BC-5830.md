<a id="BC-5830-task-1"></a>
## BC-5830 — 2026-04-22 — BC-5829 factual-anchor check #7 (cross-skill schema contracts at Plan gate) applied successfully for the 2nd time; caught review-stage P1 about metrics-from-wrong-artifact-section

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- `docs/precedents/BC-5829.md` — check #7 definition: "cross-check sibling output fields at Plan gate"
- `plugins/marketing/skills/campaign-analysis/SKILL.md` (384 lines, full live-read) — upstream primary-input contract: 6-section report template at §6 with Quick Health Check / Segment Performance Ranking / Infrastructure Analysis / Reply Sentiment / Attribution / Next Iteration subsections
- `plugins/marketing/skills/message-market-fit/SKILL.md` (483 lines, full live-read) — downstream consumer contract: ITERATE Step 4 Results Log schema with Transferable Insight column; line 267 aspirational reciprocity claim
- `plugins/marketing/skills/account-research/SKILL.md` + `evals/evals.json` — structural exemplar for tool table format and evals shape
- Filesystem probes: `docs/marketing-context.md` absent; `docs/campaigns/{entity}/` absent
- `docs/plans/marketing-gtm-expansion.md` §2.4 — scoping rationale confirmation
- `docs/precedents/INDEX.md` — engineering-side decision-trace pattern (parallel to this skill's marketing-flywheel cognate)

**Alternatives Considered:**
1. **CHOSEN.** Apply BC-5829 check #7 as a Plan-gate step: live-read every sibling skill whose output this skill consumes AND every sibling skill that consumes this skill's output. Extract every field name the contract requires (`Reply Rate`, `Interested Rate`, `Bounce Rate` verbatim from `campaign-analysis` §3.3; `transferable_note` as the MSPA-readable YAML field; 6-section artifact layout). Encode the contract in a "Cross-skill schema contract" section of the plan before any skill body is drafted. Outcome: the plan explicitly lists upstream and downstream field names; Batch D of execution writes §3 Methodology referencing those exact fields. Review caught ONE remaining P1 (§5 Workflow 1 Step 3 cited artifact §3 Infrastructure Analysis instead of §1 Quick Health Check for headline rates); validator confirmed. Fixed in review-cycle commit. Reason: cheaper than runtime failure, the review cycle is the backstop that catches any contract reference the Plan gate missed.
2. Rely on validation-by-build (validate.sh) to catch contract violations. Rejected — validate.sh checks frontmatter + section order + JSON shape; it does NOT know "Reply Rate lives at §1, not §3". Cross-skill field contracts are a skill-body concern that only review agents or runtime-failure can catch.
3. Write cross-skill integration tests that mock a real `analysis-*.md` artifact and assert campaign-debrief extracts the right fields. Rejected — heavier investment for a markdown skill; the behavioral-test scenarios in `evals/evals.json` already specify "Reply Rate 1.4%, Interested Rate 28%, Bounce Rate 2%, sent=1200, days=14" as mocked input, which is equivalent to a smoke test. The Plan-gate-plus-review-agent combination is the right granularity.
4. Skip cross-skill contract work entirely; assume sibling skills won't change. Rejected — BC-5829 precedent was explicit that schema drift is the next class of factual-anchor failure, and BC-5830's primary input IS another skill's artifact. Skipping check #7 would have shipped the §3-Infrastructure-Analysis bug directly to operators.

**Outcome:**
- Files changed: `docs/plans/BC-5830-plan.md` (Plan-gate live-read section, cross-skill schema contract section); `plugins/marketing/skills/campaign-debrief/SKILL.md` §4 Cross-skill boundaries + §5 Workflow 1 (fixed at review time to cite `artifact §1 Quick Health Check` not `§3 Infrastructure Analysis`, plus explicit "do NOT pull from §3" guard).
- Tests: 0 added, 0 passed, 0 failed (pure markdown skill; evals.json exercises cross-skill contract via `post-analysis-happy-path` mocked input).
- Approved by: auto-verified (code-reviewer confirmed the §3/§1 metrics-section finding at 9/10 confidence; Opus validator subagent CONFIRMED the claim against campaign-analysis's actual §6 §1 Quick Health Check template); 6 of 6 P1s fixed in one commit.

**Precedent Referenced:** BC-5829 (factual-anchor recipe check #7 — cross-skill schema contracts at Plan gate). This is the **second successful application** of check #7 — confirms the pattern is durable across a second skill that consumes sibling output. Extends the factual-anchor recipe by demonstrating that even with a thorough Plan-gate live-read, one section-number reference can slip through (the `§3 Infrastructure Analysis` misattribution) — meaning the review cycle remains necessary as backstop. Plan-gate + review-agent combination catches what either alone misses.

**Tags:** pattern-choice, factual-anchor-recipe, cross-skill-schema-contract, plan-gate, sibling-two, marketing-skill

---

<a id="BC-5830-task-2"></a>
## BC-5830 — 2026-04-22 — Review Step 6 validators must verify the precedent cited, not just the claim; caught a wrong-analogy P1 that would have broken BC-5761 one-question-per-field convention

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- performance-reviewer output: P1 [8/10] "Five serial AskUserQuestion calls for Q1-Q5 likely blow the under-5-minute budget" citing `account-research` §5 Workflow 1's "parallel calls in a single assistant turn" as precedent
- Opus validator subagent dispatched in `/workflows:review` Step 6 with instruction to confirm (a) AskUserQuestion tool supports multi-question batches, (b) sibling skills show an AskUserQuestion parallelization precedent, (c) Q1/Q2/Q3/Q5 are truly independent
- `plugins/marketing/skills/account-research/SKILL.md` §5 Workflow 1 — parallel-call guidance is explicitly about **`WebSearch`** (read-only query tool), not `AskUserQuestion` (blocks on operator input)
- BC-5761 precedent — "one-question-per-field rule for AskUserQuestion" — referenced in `campaign-analysis`, `creative-angles`, and other sibling skills as an established convention
- Q2/Q5 auto-suggest logic in campaign-debrief §6 Procedure 1 Steps 2-6 — Q2's result token derives from Q1's confirmed hypothesis framing; Q5's transferable insight derives from Q3's Worked/Didn't bullets

**Alternatives Considered:**
1. **CHOSEN.** Validator independently verifies the precedent the reviewer cited, not just the claim. When the cited precedent doesn't support the claim (e.g., WebSearch parallelization is not an analogy for AskUserQuestion batching because the tools have different blocking semantics), DOWNGRADE the finding. Outcome: P1 "Five serial AskUserQuestion calls" downgraded to P2 optimization. Preserved BC-5761 one-question-per-field convention across the marketing-skill family. Documented in BC-5830 Procedure 1 that Q1-Q5 remain sequential. Reason: reviewers (even specialist agents) can pattern-match on surface analogies; the validator's job is to verify the analogy holds.
2. Trust the reviewer's claim at face value and apply the fix as a P1. Rejected — this would have committed a "fix" that batches Q1-Q5 into one AskUserQuestion call, which contradicts the established BC-5761 rule and would have created silent cross-skill drift (3 sibling skills use sequential one-per-field). The fix would pass validate.sh (markdown is happy either way) and only surface at runtime when a future contributor tries to apply BC-5761 consistently across the family and finds BC-5830 as the outlier.
3. Skip Step 6 validation to save time. Rejected — 3 P1s validated in this session, 2 CONFIRMED and 1 DOWNGRADED. The one downgrade was the one that would have caused the most drift. The cost of Step 6 (one Opus subagent per P1, ~30-60s each in parallel) is small relative to the cost of a silent convention violation.
4. Have the validator ONLY confirm the claim (true/false), without verifying the precedent reasoning. Rejected — the original protocol. This session demonstrated that claim-only validation misses false-analogy P1s, because the claim "5 sequential calls waste time" is technically true; the precedent-level reasoning is where the downgrade lives.

**Outcome:**
- Files changed: none in this session (the DOWNGRADE meant the fix was NOT applied to `SKILL.md`). Review-report only: reclassified from P1 to P2 in the final report; logged the validator's reasoning in the review summary.
- Tests: 0 added, 0 passed, 0 failed.
- Approved by: auto-verified (validator's downgrade reasoning was explicit: "the claimed sibling-skill precedent is for WebSearch not AskUserQuestion, the actual AskUserQuestion pattern across campaign-analysis/message-market-fit/creative-angles is the BC-5761 one-question-per-field rule"). Downgrade accepted without operator override.

**Precedent Referenced:** BC-5761 (iteration-budget precedent) indirectly — the validator surfaced BC-5761's one-question-per-field sibling-convention status. This precedent **extends the `/workflows:review` Step 6 validation protocol** by making it explicit that validators should verify the cited precedent's applicability, not just the claim's surface-level accuracy. Previous precedents (BC-5795, BC-5797, BC-5798) established post-merge / pre-merge review catching; BC-5830 adds the finding that validators catch a new class of failure when the reviewer's precedent-reference is false-analogy.

**Tags:** pattern-choice, review-validation, precedent-citation, false-analogy, bc-5761-convention, review-protocol
