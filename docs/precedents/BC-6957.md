# BC-6957 — flow-architecture: implement `flow-preflight` skill

> **Related:** BC-6956 (predecessor — 4 helpers consumed), BC-6958 task-2 + task-3 (precedents this session counts as 2nd surface for), BC-6962 / BC-6963 / BC-6964 / BC-6965 (4 orchestrator issues this skill unblocks).
> **Linear:** [BC-6957](https://linear.app/brite-nites/issue/BC-6957/flow-architecture-implement-flow-preflight-skill) — In Review
> **PR:** [#270](https://github.com/Brite-Nites/britenites-claude-plugins/pull/270)
> **Date:** 2026-05-11

<a id="BC-6957-task-1"></a>
## task-1 — Mirror canonical helper-script impl in SKILL.md pseudocode, not just cite it

**Decision:** When a SKILL.md describes a filesystem mutation that has a sibling helper-script implementation (e.g., `scripts/flow-resume-breadcrumb.sh`'s `cmd_write`), the pseudocode block MUST mirror the helper's exact contract — same ordering, same cleanup style, same parse-verify-before-mv discipline — not invent a parallel-but-different shape. Drift between spec and helper is the failure mode reviewers catch.

**Category:** architecture

**Confidence:** 8/10

**Inputs:**
- BC-6956 task-1 / task-2 / task-3 precedents (the canonical helper contract this skill consumes)
- `plugins/flow-architecture/scripts/flow-resume-breadcrumb.sh:118-187` (cmd_write reference impl)
- /workflows:review thorough — security P2/8 heredoc injection + simplify S5 P3/7 trap-cleared-before-verify, both filed because the v1 Section 4.4 diverged from cmd_write

**Alternatives Considered:**
1. (Chosen) Rewrite Section 4.4 to mirror cmd_write exactly: `python3 json.dump()` + env-var passthrough + explicit if-checks + parse-verify before mv + content-match after. Reason: helper IS the contract; spec divergence is drift.
2. Keep heredoc + trap-EXIT shape with a `cmd_write also does this` cite. Rejected — review caught both issues independently, and the JSON injection class for Linear-controlled `$PROJECT_NAME` is a real exploit primitive even within the trusted-org threat model.
3. Defer atomic-write detail to "see cmd_write" without inline pseudocode. Rejected — orchestrator LLMs need the inline block to copy from; pure-citation loses the substance.

**Precedent Referenced:**
- BC-6956 task-2 (mktemp same-dir XXXXXX for atomic-rename) — first surface
- BC-6956 task-1 (soft-fail with sentinel; explicit if-checks over trap) — first surface
- ADR-008 (tam-mapping enrichment pluggability) — adjacent reference for "spec mirrors canonical impl" pattern, but in a different surface (provider-selection, not filesystem)
- None — first time encountering the "SKILL.md mirrors helper exactly" pattern at the architecture level (vs ad-hoc cite-and-diverge)

**Tags:** spec-mirrors-impl, skill-pseudocode-discipline, canonical-helper, cmd_write, drift-surface

**Outcome:**
- Files changed: `plugins/flow-architecture/skills/flow-preflight/SKILL.md`
- Tests: 22 AC grep checks pass; `./scripts/validate.sh` 0 errors; independent PR re-review confidence 9/10 clean
- Approved by: auto-verified (4-level verification across simplify + security + perf + cdr + independent PR review)

---

<a id="BC-6957-task-2"></a>
## task-2 — 2nd surface of BC-6958 task-2 worktree-rebase-before-PR

**Decision:** Pre-push rebase onto `origin/main` is now a 2nd-surface execution of the BC-6958 task-2 pattern. This session: rebased cleanly when BC-7011 PR #268 (marketing/aiark) merged during the work; no conflicts because the touched paths were disjoint. Pattern advances toward "ready for promotion at 6th surface" — counter at 2/6.

**Category:** pattern-application

**Confidence:** 8/10

**Inputs:**
- BC-6958 task-2 precedent text (rebase-before-PR; cross-PR drift; ship-step-1 enhancement candidate)
- `git fetch origin main && git log origin/main..HEAD` output showed 2 new commits during BC-6957 work (cd837c5 + 48effd5)
- `git rebase origin/main` → clean (no conflicts; only `.claude-plugin/marketplace.json` touched both sides, but different plugin entries)

**Alternatives Considered:**
1. (Chosen) Rebase before push. Reason: BC-6958 task-2 prescribed it; this is the 2nd validation in real conditions.
2. Push without rebase. Rejected — would have produced a stale-base PR diff and required GitHub to compute the merge against an older origin/main snapshot.
3. Merge origin/main into the branch instead of rebase. Rejected — linear history convention per repo precedent (recent commits use rebase-then-merge-commit style).

**Precedent Referenced:**
- BC-6958 task-2 — first surface (2026-05-11)
- This session — 2nd surface (2026-05-11, same day; both UI-bearing plugin-work sessions on different days would show stronger temporal-pattern signal)

**Tags:** worktree-rebase, 2nd-surface, ship-step-1, pre-push-discipline, BC-6958-cluster

**Outcome:**
- Files changed: none (process precedent; affected commit ordering, not content)
- Tests: post-rebase `./scripts/validate.sh` 0 errors; AC greps still all PASS
- Approved by: auto-verified (clean rebase + clean post-rebase validate)

---

<a id="BC-6957-task-3"></a>
## task-3 — 2nd surface of BC-6958 task-3 Auto Mode P3 fix-application

**Decision:** Auto Mode + `/workflows:review` → apply ALL validator-confirmed P2/P3s in same PR when behavior-preserving + trivial. This session: 7 P2s + 4 P3s validated; 3 confirmed P3s applied + 1 downgraded; all behavior-preserving. Pattern advances toward promotion — counter at 2/6.

**Category:** pattern-application

**Confidence:** 8/10

**Inputs:**
- BC-6958 task-3 precedent text (3-gate override of P1-only review spec: Auto Mode + behavior-preserving + trivial)
- This session's validator output: A:CONFIRMED, B:DOWNGRADED, C:CONFIRMED, D:CONFIRMED (Sonnet batched validator)
- Recent `feedback_honor_user_gate_selection.md` memory + Auto Mode preamble

**Alternatives Considered:**
1. (Chosen) Apply confirmed P3s in same PR (A: Write tool dropped; C: mode-table wording; D: hardcoded version dropped). Reason: BC-6958 task-3 explicit; Auto Mode in play; all behavior-preserving + < 5-line each.
2. Defer P3s to follow-up PR. Rejected — review-iter churn cost outweighs single-PR clarity per BC-6958 task-3 reasoning.
3. Apply only the highest-confidence (CR-1 @ 8/10), defer the rest. Rejected — DOWNGRADED finding B is the only one not applied; all CONFIRMEDs were applied uniformly per the precedent.

**Precedent Referenced:**
- BC-6958 task-3 — first surface (2026-05-11)
- This session — 2nd surface (2026-05-11)

**Tags:** auto-mode, review-p3-application, 2nd-surface, behavior-preserving, BC-6958-cluster

**Outcome:**
- Files changed: `plugins/flow-architecture/skills/flow-preflight/SKILL.md` (3 P3 fixes folded in)
- Tests: AC greps re-verified after each P3 application; `./scripts/validate.sh` 0 errors throughout
- Approved by: independent PR re-review (clean, confidence 9/10)
