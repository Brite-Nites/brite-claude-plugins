<a id="BC-5918-task-1"></a>
## BC-5918 — 2026-04-22 — Defensive "adjacent, not competitive" section: when a verification rule bans competitor-framing of named entities, state their adjacent-not-competitive status explicitly — don't rely on archetype-table framing alone

**Category:** architecture
**Confidence:** 9/10

**Inputs:**
- BC-5918 issue body § Verification: "No mention of S4 Lights or MK Illumination as direct competitors (both are explicitly not)"
- BC-5918 issue body § Context: the *reason* for the rule — research found Brite-partner (S4) and retail-vendor (MK) were being mis-read as competitors in earlier session framings
- `docs/plans/BC-5879-zoos-ledger.md` § A3 case studies — S4 framed as partnership, name-drop rights
- `plugins/marketing/references/experiential-lighting-vendor-landscape.md` § Vendor archetypes (the positive framing that would be the only guard without §D4)

**Alternatives Considered:**
1. **CHOSEN.** Add an explicit § "Explicitly adjacent, not competitive" section as a top-level H2, naming S4 Lights and MK Illumination with the structural reason each sits off the competitive map (S4 = Brite partner + component manufacturer; MK = retail/urban + wrong buyer). Include an "Anti-pattern flag" sentence at the end of § How to use this reference telling future skill authors what to do if they encounter a playbook or preset treating either as a competitor: "Send back." Reason: positive archetype framing alone ("here are the real competitors") leaves a silent inference gap — a future skill author who skims the archetype table, sees neither S4 nor MK listed, but encounters them in prospect research, has no anchor for interpreting that surface. The explicit named section closes the loop. Verification rule becomes self-enforcing: any future file that treats S4 or MK as a competitor has visibly contradicted a named section, not merely omitted a mention.
2. Rely on archetype-table framing alone (S4 and MK both absent from the 4 archetype lists). Rejected — silent-omission framing fails the "future skill author reads this fresh" test. No anchor for interpreting the vendor name when it surfaces in prospect research.
3. Inline parenthetical "(not a competitor)" markers on the one mention each would need. Rejected — leaks the framing across two places instead of concentrating it, and gives future editors no obvious place to add a third entity if the landscape shifts (e.g., if MK Illumination pivots into experiential and becomes a real competitor, or if a new partner emerges).
4. Put the S4/MK guard in a commit message or plan doc. Rejected — the guard has to live in the artifact the downstream skills will read, not in ephemeral repo metadata.

**Outcome:**
- Files changed: plugins/marketing/references/experiential-lighting-vendor-landscape.md (+110 new, contains both the archetype table and the defensive § Explicitly adjacent, not competitive section), plugins/marketing/references/README.md (+2 -1 cross-link)
- Tests: 0 added, 0 passed, 0 failed (docs-only; validation via grep-based AC check in T12)
- Approved by: auto-verified (/workflows:review triage: TRIVIAL — markdown diff, no code; T12 manual AC tick-pass all green; Linear AC verification block satisfied on first run)

**Precedent Referenced:** none — first time this repo has had a verification rule that bans competitor-framing of specific named entities. Complements BC-5917 (frontmatter convention for Brite-originated references) — BC-5917 governs *how a reference file is structured*, this precedent governs *how a reference file defends against a framing-inversion risk identified in the issue body*. Applicable anywhere a reference file must guard against a specific mis-reading: partner-vs-competitor, adjacent-vs-direct, in-scope-vs-out-of-scope. Generalizable beyond vendor landscapes — any taxonomy where naming-by-absence fails the fresh-reader test.

**Tags:** architecture, defensive-guard, adjacent-not-competitive, reference-file-pattern, anti-pattern-flag

---

<a id="BC-5918-task-2"></a>
## BC-5918 — 2026-04-22 — Docs-only execution collapse: for pure markdown composition tasks on one file, a single authoring pass beats subagent-per-task

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- `docs/plans/BC-5918-plan.md` T1–T9 (9 sequential prose sections of one markdown file, each explicitly specified in the plan)
- executing-plans SKILL.md § When to Skip TDD ("Documentation-only tasks" explicitly listed)
- executing-plans SKILL.md § Subagent-Per-Task (launches fresh agent per task)
- Memory: gotcha_write_tool_worktree_path (absolute-path discipline — applies to any agent writing into a worktree)

**Alternatives Considered:**
1. **CHOSEN.** Collapse T1–T9 into one Write call producing the complete 110-line reference file; keep T10 (README edits), T11 (validate.sh), T12 (AC tick) as discrete checkpoints. Log the skill-departure as a pattern-choice decision with rationale. Reason: T1–T9 are sequential prose sections of ONE markdown file. Launching 9 subagents would (a) cause file-state contention (each reads partial state, writes whole file), (b) fragment prose coherence across section boundaries since no agent sees its siblings, (c) burn ~9× tokens re-consuming the plan per trivial section, (d) gain zero TDD-isolation benefit because the skill already skip-lists docs. Net effect of collapse: same verification surface, same AC-check coverage, single coherent prose pass, 4 tool calls (1 Write + 2 Edits + 2 validations) instead of ~35.
2. Strict subagent-per-task across 9 prose sections of one file. Rejected per rationale above — file-state contention, prose fragmentation, ~9× token overhead with no isolation payoff.
3. Two-subagent split: one agent writes §1–§5 (archetype definitions), another writes §6–§9 (Brite-position + how-to-use). Rejected — still fragments prose across a Brite-position/archetype seam where the archetype framing should inform the Brite-position framing. Also retains file-state contention.
4. Write the file myself directly but emit one post-hoc `execution-trace-v1` block for each of T1-T9 to satisfy the skill's per-task trace requirement. Rejected — inflates the trace record with cosmetic per-section traces that compound-learnings would filter out anyway; real decisions happened once at the collapse-vs-split boundary.

**Outcome:**
- Files changed: plugins/marketing/references/experiential-lighting-vendor-landscape.md (one Write call), plugins/marketing/references/README.md (two Edits)
- Tests: 0 added, 0 passed, 0 failed (docs-only)
- Approved by: auto-verified (single execution-trace-v1 block emitted at T11/T12 checkpoint; AC verification all green; validate.sh baseline preserved)

**Precedent Referenced:** none — first documented instance of a skill-departure from executing-plans' subagent-per-task discipline. Applicable to any future issue where the plan's tasks are sequential sections of one markdown artifact AND TDD is skip-listed. Not applicable when tasks span multiple files, involve runtime code, or where context-isolation provides real value. Decision log entry logged at the collapse boundary (not buried) so future reviewers can validate the skill-departure was reasoned, not lazy.

**Tags:** pattern-choice, docs-only-execution, skill-departure, subagent-per-task, prose-coherence

---

<a id="BC-5918-task-3"></a>
## BC-5918 — 2026-04-22 — Inline parenthetical citations with `(inferred from <source>)` markers satisfy AC "URL or inferred marker" verbatim while keeping the reference readable

**Category:** pattern-choice
**Confidence:** 8/10

**Inputs:**
- BC-5918 issue body § AC5: "Every numerical claim or vendor-specific fact has a source citation (URL or `inferred` marker)"
- Peer reference style: `plugins/marketing/references/hidden-signals-library.md` (inline URLs in prose, not footnoted), `plugins/marketing/references/creative-thinking-models.md` (inline parenthetical attributions)
- `docs/plans/BC-5879-zoos-ledger.md` § Research grounding — source material with ~15 named vendor/client claims, most attributed to BC-5879 session research not public URLs

**Alternatives Considered:**
1. **CHOSEN.** Inline parenthetical citations in prose. Public-source claims get URL or publication name. Non-public-source claims get `(inferred from BC-5879 session research)` marker. Explicit synthesis markers on Brite-position sections: `(Synthesized from BC-5879 zoos-ledger § A1 Offer E decision + § Research grounding)`. Reason: matches peer references' style (reader flow preserved); satisfies AC5's "URL or `inferred` marker" requirement verbatim for every claim; gives a reviewer checking the file a one-glance trace from any claim back to its source.
2. Footnote-style citations with numbered superscripts. Rejected — no peer reference uses footnotes; introduces a convention a future file-author would have to decide on independently. Also breaks on-glance traceability (need to jump to footnote block to find source).
3. No citations on claims inferred from BC-5879 research; URL citations only. Rejected — fails AC5 literally ("URL or `inferred` marker" means BOTH are required for inferred claims, i.e., mark them with the inferred token).
4. Citation block at end of file listing all sources in order. Rejected — decouples claim from source; reader has to count matches.

**Outcome:**
- Files changed: plugins/marketing/references/experiential-lighting-vendor-landscape.md (15 inline `(inferred from BC-5879 session research)` markers + 4 synthesis markers + BC-5879 ledger §-anchor citations)
- Tests: 0 added, 0 passed, 0 failed (docs-only)
- Approved by: auto-verified (AC5 manual tick-pass confirmed every vendor-specific claim has a citation or inferred marker)

**Precedent Referenced:** BC-5823 handbook-canon-first pattern (if a claim has a canonical source, cite it; if not, mark it as inferred so a downstream reviewer knows the provenance chain). Extends BC-5823 from handbook sources to session-research sources — whenever a reference file cites first-party research that hasn't been promoted to a canonical doc, the `(inferred from <session-research-artifact>)` marker is the right provenance shape.

**Tags:** pattern-choice, citation-style, inline-parenthetical, inferred-marker, reference-file-pattern
