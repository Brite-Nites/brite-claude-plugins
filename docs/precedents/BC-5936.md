<a id="BC-5936-task-1"></a>
## BC-5936 — 2026-04-23 — Em-dash AC for preset files is file-wide strict, not body-only; author em-dash-free from first draft (including Hook/Step-1/Step-2 prose + anti-slop bullets), not just body templates

**Category:** pattern-application
**Confidence:** 7/10

**Inputs:**
- Issue BC-5936 AC wording: `grep -l '—' [ski-resorts preset files]` returns no files — file-wide, not scoped to `**Body:**` sections
- SKILL.md §8 anti-slop rule wording: "Do not emit em-dashes (`—`) in body copy. Known EB spam trigger. Auto-replace with commas, periods, or hyphens at draft time. Hard failure if present in the written artifact. (Em-dashes in this SKILL.md spec prose are OK where explaining the rule; the rule applies only to body template examples and generated artifact text.)" — reading this in isolation, "body copy" reads as scoped to body templates only, not file-wide
- Empirical cross-check of existing preset files (BC-5936 T2 mid-execution):
  - `grep -l '—'` on zoos presets (list-building-zoos.md, risk-reversal-zoos.md, list-building-zoos-pilot-zone.md, risk-reversal-zoos-pilot-zone.md) → 0 matches (all em-dash-free file-wide)
  - `grep -l '—'` on hotels presets (list-building-hotels-resorts.md, risk-reversal-hotels-resorts.md, + `-rate-premium` variants) → 0 matches (all em-dash-free file-wide)
  - `grep -l '—'` on municipalities seed presets (list-building-municipalities.md, risk-reversal-municipalities.md, BC-5825 era) → matches (both files have em-dashes)
- BC-5936 T2 authored `list-building-ski-resorts.md` with 5 em-dashes — all in spec prose (Hook section intro, Step 1 skeleton intro, Step 2 bump intro, anti-slop bullets) — zero in body templates. Caught at first verification grep, fixed with Edit substitutions (em-dash → colon/comma/semicolon), then re-authored T3/T4/T5 em-dash-free from first draft.

**Alternatives Considered:**
1. **CHOSEN.** Treat the AC grep as strict file-wide: author preset files with zero em-dashes anywhere — body templates AND surrounding spec prose (Hook / Step 1 / Step 2 introductions + Vertical anti-slop bullets). Use colons, commas, semicolons, or hyphens for every pause a writer might naturally em-dash. Reason: zoos R-10 (BC-5921) + hotels R-7/R-13 (BC-5921/BC-5935) peer precedent already ships files em-dash-free file-wide, establishing the ground-truth pattern before the issue AC made it explicit. The SKILL.md §8 rule is ambiguous (reads as body-only) but the issue AC resolves the ambiguity file-wide. First-draft discipline avoids retroactive Edit churn.
2. Interpret SKILL.md §8 as body-only and allow em-dashes in spec prose; fix only when the issue AC grep fails. Rejected — the AC is file-wide, so the retroactive-fix path always runs. Front-loading the discipline at authoring time saves 5+ Edit calls per preset file and prevents the "forgot one em-dash in an anti-slop bullet" near-miss at final verification.
3. Author preset spec prose with em-dashes freely and rely on a pre-commit auto-replace hook. Rejected — no such hook exists in the repo (`scripts/_lib/lint_hooks.py` handles hook-file model-ID enforcement, not markdown em-dashes). Tooling cost > human-author discipline cost at current preset cadence.

**Outcome:**
- Files changed: `plugins/marketing/skills/email-copywriting/presets/list-building-ski-resorts.md` + `risk-reversal-ski-resorts.md` + `list-building-ski-resorts-pilot-zone.md` + `risk-reversal-ski-resorts-pilot-zone.md` (all 4 files em-dash-free file-wide at commit time)
- Tests: `grep -l '—' plugins/marketing/skills/email-copywriting/presets/*ski-resorts*.md` → 0 matches (AC passes); `./scripts/validate.sh` 0 errors / 16 warnings baseline preserved; `./scripts/check-guardrails.sh` 0 violations
- Approved by: user plan approval 2026-04-23 + T6 verification gate + TRIVIAL diff-triage ratification

**Precedent Referenced:** None (net-new pattern-application precedent). Establishes the file-wide em-dash-free discipline as the preset-composition default, adding a recipe check to the preset-composition playbook. **Next applicability:** every future preset composition (BC-5933 aquariums, BC-5937 sports-stadiums, BC-5934 casinos, BC-5880 Exploring-tier fan-out, BC-5881 Future-tier fan-out) inherits this discipline — author em-dash-free from first draft across Hook + Step 1 skeleton + Step 2 bump + Vertical anti-slop sections, not just body templates. **Promotion criterion:** if a second preset-composition session independently rediscovers the file-wide scope at verification time (repeating the BC-5936 T2 mid-execution discovery pattern), promote to pattern-application 8/10 and add to the preset-composition template as an explicit first-draft checklist item. If a third session repeats the discovery, promote to architecture 9/10 and consider updating SKILL.md §8 prose to disambiguate "body copy" → "file-wide including spec prose."

**Tags:** pattern-application, em-dash-discipline, file-wide-scope, preset-composition, first-draft-recipe

---
