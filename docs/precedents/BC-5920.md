<a id="BC-5920-task-1"></a>
## BC-5920 — 2026-04-22 — Vertical-playbook reference files restate the adjacent-not-competitive guard per-vertical, not just at the top-level file

**Category:** pattern-application
**Confidence:** 7/10

**Inputs:**
- BC-5918 precedent task-1 (adjacent-not-competitive defensive guard, architecture 9/10) — established the guard at `experiential-lighting-vendor-landscape.md` (top-level, vertical-agnostic)
- BC-5920 issue body § Section 1 — "Vendor landscape for zoos (sub-distilled from R-2, with zoo-specific lens)"
- BC-5879 zoos-ledger § A3 case studies (S4 partnership framing)
- `plugins/marketing/references/experiential-lighting-vendor-landscape.md` §§ Vendor archetypes + Explicitly adjacent, not competitive (top-level guard already present)

**Alternatives Considered:**
1. **CHOSEN.** Restate the adjacent-not-competitive guard as a zoos-scoped sub-section (`### Explicitly adjacent, not competitive (zoos-specific)`) inside § Vendor landscape of `vertical-playbooks/zoos.md`. S4 Lights + MK Illumination both named again, with zoos-context framing (S4 = Brite partner for DMX-pixel supply chain; MK = retail/urban streetscape, wrong motion at zoos). Reason: the top-level guard in the vendor-landscape file is vertical-agnostic and anchors the archetype taxonomy; the per-vertical restatement is where prospect research actually happens. A future skill composing a zoos-specific preset reads the zoos playbook, not the top-level vendor-landscape file — if the guard only lives at the top level, the prospect-research loop can miss it. Restating the guard at the vertical level closes the same silent-inference gap BC-5918 identified, one level down.
2. Omit the per-vertical restatement and rely on the top-level guard only. Rejected — fails the same fresh-reader test BC-5918 applied to archetype framing. A future skill author working on a zoos preset may never open the vertical-agnostic file; the guard has to live at the level where prospect decisions happen.
3. Move the guard entirely into the per-vertical file and drop the top-level guard. Rejected — BC-5918's architecture decision was to anchor the guard at the vertical-agnostic level so all future playbooks (aquariums, casinos, hotels, ski-resorts, stadiums) inherit the same named-exclusion discipline. Removing it would require each playbook to independently rediscover the framing.
4. Restate only S4 (the one with the partnership framing) and omit MK at the vertical level. Rejected — BC-5918's rationale applies equally to MK: a zoos-vertical author who encounters MK in prospect research needs the same anti-pattern flag. Partial restatement leaks the framing asymmetrically.

**Outcome:**
- Files changed: plugins/marketing/references/vertical-playbooks/zoos.md (new, 240 lines — includes `### Explicitly adjacent, not competitive (zoos-specific)` sub-section inside § Vendor landscape); plugins/marketing/references/README.md (+2 lines: contents entry + consumer entry); docs/plans/BC-5920-plan.md (new, 15-task plan)
- Tests: 0 added, 0 passed, 0 failed (docs-only; AC verified via 8-check grep pass)
- Approved by: auto-verified (TRIVIAL triage matches BC-5918 PR #177 shape; validate.sh 0 errors / 16 warnings baseline preserved)

**Precedent Referenced:** BC-5918 task-1 (adjacent-not-competitive defensive guard, architecture 9/10). This precedent is a direct pattern-application of BC-5918's principle, extending it from the vertical-agnostic file to the per-vertical file. Applicable to all future vertical playbooks (aquariums BC-5929, casinos BC-5930, hotels BC-5921, ski-resorts BC-5922, sports-stadiums BC-5923) — each should restate the S4 + MK guard in its own § Vendor landscape with vertical-specific framing. Generalizable beyond vendor landscapes: any defensive guard that crosses a taxonomy level (vertical-agnostic → per-vertical, per-vertical → per-preset) must restate at each level, not just the highest.

**Tags:** pattern-application, adjacent-not-competitive, per-vertical-restatement, reference-file-pattern, bc-5918-extension

---

<a id="BC-5920-task-2"></a>
## BC-5920 — 2026-04-22 — Check-in-gate scaffolding in the plan makes partially-pending source ledgers shippable without inventing content

**Category:** pattern-choice
**Confidence:** 7/10

**Inputs:**
- BC-5879 zoos-ledger (source material): Tier A partial (§ A1 Offer E + § A3 case studies + Offer A decision written; § A2 buyer persona, § A4 differentiator, Tiers B/C/D/F all marked "Pending")
- BC-5920 issue body § Tasks — requires all 9 sections (Voice rules, Anti-slop rules, Buyer personas) be written now, not deferred
- executing-plans skill discipline — "Stop and ask if anything is ambiguous"
- feedback_one_question_at_a_time memory (AskUserQuestion discipline at checkpoints) + feedback_research_before_implementation memory

**Alternatives Considered:**
1. **CHOSEN.** Embed three explicit check-in gates in the plan (Gate 1 before § Buyer personas, Gate 2 before § Voice rules, Gate 3 before § Anti-slop rules) — each with 2-4 specific questions derived from the ledger gap. During execution, front-load all three gates into one AskUserQuestion batch since they affect adjacent sections and batching saves round-trips. Reason: the plan document is the right place to enumerate ambiguity-risk surfaces — a reviewer reads the plan and sees "these are the places the ledger is silent and the operator must weigh in." Execution-time front-loading is defensible when the gates aren't conditional on each other (no gate's answer changes the next gate's question set).
2. Full brainstorm session before planning — Socratic discovery of the pending ledger tiers. Rejected — complexity criteria (single-module, established pattern, <3 step new-ness) don't warrant a brainstorm; check-in gates in the plan cover the same ground at lower cost.
3. Write the playbook with placeholder `[TODO: confirm with operator]` markers in each pending section. Rejected — leaves the reference file in a half-built state; downstream skills reading the playbook would get partial content and fall back to guessing.
4. Defer Buyer personas / Voice rules / Anti-slop to follow-up issues (BC-5920b / BC-5920c / BC-5920d). Rejected — the issue AC explicitly requires all 9 sections present in this PR; deferring fails AC2.
5. Single-gate at plan-approval time, then write everything without in-execution stops. Rejected — gate questions were specific enough that the operator needed to see the draft-ready rule phrasing, not just approve the plan abstractly. In-execution gates with concrete proposed phrasing produced higher-fidelity answers.

**Outcome:**
- Files changed: docs/plans/BC-5920-plan.md (15-task plan with 3 explicit gates documented)
- Tests: 0 added, 0 passed, 0 failed (docs-only)
- Approved by: operator at single batched AskUserQuestion (4 questions, 4 answers); all 4 answers shaped final prose — persona list kept verbatim, sponsors cited by name, Voice rule #3 rephrased from exclusion to ordering, Anti-slop rule #5 softened from prohibition to sequencing

**Precedent Referenced:** BC-5918 task-2 (docs-only execution collapse, pattern-choice 8/10) — this precedent complements BC-5918's collapse decision. Where BC-5918 established *one Write for sequential prose*, BC-5920 establishes *gate-before-Write when the source material has unresolved questions*. Both sit on the same axis: docs-only execution is not vacuously TDD-skippable; it still benefits from structured checkpoints where ambiguity lives. Applicable to any future issue where the scoping source (ledger, research doc, design artifact) is known to be partially complete at execution time. Not applicable when the source is self-contained — don't add gates for aesthetic approval.

**Tags:** pattern-choice, check-in-gate, plan-scaffolding, partial-ledger, in-execution-gate

---

<a id="BC-5920-task-3"></a>
## BC-5920 — 2026-04-22 — AC literal-string traps: verification rules that ban a phrase are broken by quoting that phrase in the rule itself

**Category:** bug-resolution
**Confidence:** 6/10

**Inputs:**
- BC-5920 issue body § Verification — "No usage of 'lantern vendor' in the voice rules (uses 'existing lighting vendor' or equivalent)"
- First-pass draft of § Voice rules — rule #1 stated: *Use "existing lighting vendor", "incumbent", "current seasonal-program vendor" — not "lantern vendor" or "projection vendor" or any specific sub-category*
- AC grep check: `! grep -q "lantern vendor" zoos.md` failed because the rule body literally contained the string "lantern vendor"

**Alternatives Considered:**
1. **CHOSEN.** Rephrase the rule to model the preferred phrasing without quoting the banned phrase: *never a specific sub-category shorthand that surfaced from the vendor-landscape research (lantern-festival producer, projection-studio operator, LED-retrofit installer)*. The parenthetical enumerates *descriptive archetype names* (which are fine — they're the archetype labels from the vendor-landscape file) instead of the banned shorthand. Reason: the AC's literal grep check requires the string "lantern vendor" to be absent from the file; a rule that quotes the banned phrase fails its own enforcement. Reframing to use archetype labels (which match the vendor-landscape file's top-level H3 headings) is both more accurate (archetype label is the research-true framing) and literal-grep-passing.
2. Keep the banned-phrase quotation and argue the AC is too literal. Rejected — AC checks are run by grep, not judgment; literal-string traps are the AC's job to enforce and the implementor's job to respect.
3. Use typographical escape: `"lantern-vendor"` (hyphenated) to break the grep match. Rejected — hacky; the AC is testing that the reference file models preferred phrasing, not that the string is absent by trickery. Escape defeats the AC's intent even if it passes the literal check.
4. Move the banned-phrase examples into a separate `## Examples of what NOT to say` section and keep rule body clean. Rejected — doubles the maintenance surface and the rule body was already the right place for the discipline.

**Outcome:**
- Files changed: plugins/marketing/references/vertical-playbooks/zoos.md (one Edit to § Voice rules #1)
- Tests: 0 added, 0 passed, 0 failed (docs-only; AC7 grep check re-verified post-fix: absent)
- Approved by: auto-verified (AC7 tick-pass green after rephrase)

**Precedent Referenced:** none — first time this repo has had an AC literal-string trap of this shape. Applicable to any future issue with a "no usage of 'X'" AC: the rule body that enforces the discipline must model the preferred alternative without quoting X verbatim. Generalizes to any verification rule that uses `grep -v` as the check — quotation of the banned pattern, including in examples or "DO NOT say" framing, will fail the literal check. Likely recurrence: verification rules for banned buzzwords, banned vendor names, banned framing conventions.

**Tags:** bug-resolution, ac-literal-trap, grep-check, voice-rule, verification-rule
