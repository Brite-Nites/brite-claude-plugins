---
domain: marketing
trait: needs-marketing
last_refreshed: 2026-05-08
refresh_cadence: quarterly
generated_by: BC-6785-stub
---

# Marketing Context (stub)

> **Status:** minimal stub created during BC-6785 round-5 dogfood walk to satisfy `launch-campaign.md § Input validation § IV-5` (Phase 10 Mode 2 `--test-send` allowlist requirement). The full brand-context document is generated by the `product-marketing-context` skill and should replace this stub before any production marketing skill is invoked.
>
> **What this stub provides:** only the `## test_send_allowlist` section needed to unblock operator-self-send during dogfood. It does NOT contain entity ICPs, brand voice, sender block, or any of the other sections that downstream skills (`message-market-fit`, `creative-angles`, `email-copywriting`, etc.) read from a fully-populated marketing-context.
>
> **Next action:** run `/marketing:product-marketing-context` (or its equivalent) to regenerate this file with the full brand context. The `## test_send_allowlist` section is appendable — re-running the generator should preserve or merge the allowlist.

## test_send_allowlist

The list of recipient domains that `launch-campaign.md § Phase 10 Mode 2 --test-send` is permitted to deliver to. Per IV-5, any domain not in this list halts Mode 2 with no `--allow-external-test-send` override available. Protects against typo-misdirection (operator intends own inbox, types a customer inbox by mistake).

- `britenites.com`

**Rationale for `britenites.com`:** Brite-internal corporate domain; covers operator inboxes (e.g., `corinne@britenites.com`) and team mailboxes. Plus-addressed variants (e.g., `corinne+bc-6785-r5@britenites.com`) route to the same inbox per Gmail/M365 routing.

**To extend:** add additional Brite-internal domains as needed (e.g., a sandbox or staging domain). Do NOT add customer or vendor domains — those are external recipients and the IV-5 protection exists specifically to prevent test-sends from reaching them.
