---
description: Documents the commit-message convention. Use when explaining how to format commits.
allowed-tools: Read
---

<!-- lint:not-side-effecting prints guidance only; the git commit mention is documentation -->

# Commit convention

This reference command documents how `git commit` messages should be formatted.
It only prints guidance — it performs no git operations itself.
