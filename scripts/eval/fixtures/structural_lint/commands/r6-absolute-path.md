---
description: Loads local settings from disk. Use when applying a developer's config.
allowed-tools: Read
---

# Load config

Reads `/Users/holden/project/config.json` directly instead of `${CLAUDE_PLUGIN_ROOT}/config.json`.
