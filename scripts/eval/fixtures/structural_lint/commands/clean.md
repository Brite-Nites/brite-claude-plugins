---
description: Shows the status of the current plan. Use when reviewing progress read-only.
allowed-tools: mcp__plugin_workflows_linear-server__get_issue, Read
---

# Plan status

Runs `git status` and reads `${CLAUDE_PLUGIN_ROOT}/scripts/foo.py`.
See https://example.com/docs and plugins/workflows/commands/other.md for context.
Upstream lives at https://github.com/Users/acme/repo (a URL — the R6 lookbehind must NOT flag this).
