---
name: r8-override
description: Documents the Linear MCP tool namespace convention. Use when explaining tool naming.
user-invocable: false
---

<!-- lint:no-mcp-invocation reference skill only documents the tool name; it performs no call -->

# R8 valid override

This reference skill documents that Linear tools are named like
`mcp__plugin_workflows_linear-server__get_issue`. It only explains the naming
convention — it invokes nothing, so the non-silent override downgrades R8 to advisory.
