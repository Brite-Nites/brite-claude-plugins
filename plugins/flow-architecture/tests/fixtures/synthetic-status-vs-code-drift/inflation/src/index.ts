// Fixture code root for the BC-12909 inflation case. src/ EXISTS (so the
// repo-level "no code tree to diff" skip does NOT apply), but there is no
// implementation for BILL-01 anywhere — nothing under src/billing/. A fresh
// scan therefore infers NOT_STARTED against the doc's declared BUILT → inflation.

export const appName = "billing-demo";
