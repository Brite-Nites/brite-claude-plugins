# Stray notes file

This file has no YAML front-matter at all. The journey builder must skip it
entirely — it contributes nothing to `flow_ids_in_scope` or `personas`, and
its presence must not abort the aggregation (skip-don't-crash).
