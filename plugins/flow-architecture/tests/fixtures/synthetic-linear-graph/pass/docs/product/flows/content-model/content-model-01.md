---
flow_id: content-model-01
title: Page model
---

# Content model — page model

This flow has no cross-domain blockers (it is depended on, not depending). No
`## Cross-domain dependencies` section, by design — the blockedBy bullet lives
on the *blocked* flow's doc (seo-foundation-01), not here.
